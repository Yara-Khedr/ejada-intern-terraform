apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: yara-oke-pvc
  namespace: yara-app
spec:
  storageClassName: "oci-bv"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi